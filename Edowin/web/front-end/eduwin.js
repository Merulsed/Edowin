$(document).ready(function(){
	
	// $('.sidebar').css('left', '-270px');
	var formWidth = 1;
	$('.not-sidebar form').css('width', ($('.not-sidebar form .inputbox').width()+34)*formWidth);
	$('form width1').css('width', ($('.not-sidebar form .inputbox').width())*formWidth);

});